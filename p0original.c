#include<stdio.h>
int sum(int a , int b)
int main()
{
  int a,b,sum;
  printf("enter thr value of a and b");
  scanf(" %d %d" , &a, &b);
  sum=add(a,b);
  printf("the sum of two numbers is %d\n", sum);
  return 0;
}

int sum (int a,int b)
{
  int x, y, sum;
  sum(x+y);
  return sum;
}